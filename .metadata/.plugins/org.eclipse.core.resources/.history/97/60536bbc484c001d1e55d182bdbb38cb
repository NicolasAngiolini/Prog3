import java.util.ArrayList;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;
import java.util.ListIterator;


public class ServicioBFS {

	private Grafo<?> grafo;
	protected LinkedList<Integer> vertices;
	
	public ServicioBFS(Grafo<?> grafo) {
		this.grafo = grafo;
	}
	
	public List<Integer> bfsForest() {
	 Iterator<Integer> iterador = grafo.obtenerVertices();
	 while(iterador.hasNext()) {
		//pone en falso todos los vertices
	 }
	 
	 ArrayList<Integer> lista = new ArrayList<>();
	 
	 Iterator<Integer> it = grafo.obtenerVertices();
	 while(it.hasNext()) {
		 int verticeId = it.next();
		 if(true/*si visitado esta falso*/){
			 bfs_visit(verticeId);
		 }
				 
	 }
	 
		return vertices;
	}
		
	public void bfs_visit(int v) {
		//pone el visitado en verdadero
		vertices
		
	}
	
	}

	



